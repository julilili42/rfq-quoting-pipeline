# Samples

Put anonymized example RFQs here for demos and manual testing.

Supported formats: `.pdf`, `.eml`, `.xlsx`, `.csv`

These are **not** used by automated tests — those live in `tests/fixtures/`.
